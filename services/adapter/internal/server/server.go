package server

import "google.golang.org/grpc"

// Register registers the adapter service with the gRPC server.
// This is a stub — proto-generated code will be wired here.
func Register(srv *grpc.Server) {
	// Will register adapterv1.AdapterServiceServer once proto codegen is wired.
}
